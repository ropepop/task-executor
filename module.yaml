schema: 1
module_id: task_executor
owner: ops
runtime:
  type: external
  healthcheck: github_actions_workflow_status
  run_id_env: GITHUB_RUN_ID
components:
  - id: operation_queue_drain
    start: github workflow dispatch stalled-runner-cron.yml
    stop: n/a
    health: workflow run result
  - id: cache_refresh
    start: github workflow dispatch cache-refresh-cron.yml
    stop: n/a
    health: workflow run result
observability:
  event_source: task_executor
  evidence_root: ops/evidence/task-executor
